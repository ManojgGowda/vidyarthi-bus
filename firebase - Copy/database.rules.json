{
  "rules": {
    "routes": {
      "$routeId": {
        "reports": {
          "$uid": {
            ".read":  "auth != null",
            ".write": "auth != null && auth.uid === $uid"
          }
        }
      }
    }
  }
}
